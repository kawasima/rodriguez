{
  "name": "rodriguez-node-example",
  "private": true,
  "type": "module",
  "scripts": {
    "sqs": "node src/sqs.cjs",
    "fuse": "node src/fuse.cjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.750.0",
    "@aws-sdk/client-sqs": "^3.750.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
